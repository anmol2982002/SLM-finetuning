# Requirements for Gemma-3 Document Classification Pipeline
# =========================================================

# Core ML Libraries
torch>=2.0.0
transformers>=4.38.0
datasets>=2.14.0
peft>=0.7.0
accelerate>=0.25.0

# Quantization (optional, for low-memory training)
bitsandbytes>=0.41.0; sys_platform != 'win32'

# Document Processing
tika>=2.6.0

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Evaluation & Metrics
scikit-learn>=1.3.0
numpy>=1.24.0

# Configuration & Utilities
pyyaml>=6.0.0
tqdm>=4.65.0

# Logging (optional)
# wandb>=0.16.0
# tensorboard>=2.14.0
